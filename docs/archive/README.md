# Archived documents

Superseded documents kept so the reasoning behind earlier decisions is not
lost (see `.cursor/rules/05-documentation.mdc` §15).

| File | Superseded on | By |
| --- | --- | --- |
| `2026-08-flutter-architecture.md` | 2026-08-29 | `docs/architecture.md` (ADR-001, web-first TypeScript) |

The Flutter-era `data-model.md`, `features.md` and `roadmap.md` were short
stubs; their last versions are in git history at commit `4cf4c5a`.
